# bode API Documentation

This documentation is generated from source comments using FORD.

## Scope

- Public `bode_mod` interfaces and callbacks
- Solver entrypoint options (`iopt`)
- Error/failure semantics
- Runtime counters (`nfev`, `njev`, `nlu`, `nbsol`)

## Build docs locally

```bash
ford ford.md
```

Generated files are written under `build/docs/ford`.
